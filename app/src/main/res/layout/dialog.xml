<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <NumberPicker
        android:id="@+id/numberPicker1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="64dp" />

    <Button
        android:id="@+id/dialogCancelBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/numberPicker1"
        android:layout_marginStart="20dp"
        android:layout_marginTop="98dp"
        android:layout_toEndOf="@+id/numberPicker1"
        android:text="Cancel" />

    <Button
        android:id="@+id/dialogOrderBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/dialogCancelBtn"
        android:layout_alignBottom="@+id/dialogCancelBtn"
        android:layout_marginEnd="16dp"
        android:layout_toStartOf="@+id/numberPicker1"
        android:text="Order" />

</RelativeLayout>